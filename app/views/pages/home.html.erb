<div class="container mt-5">
  <h1>Welcome to Slack of Batch #1565 💬</h1>
    <% @chatrooms.each do |chatroom| %>
    <div class="card rounded p-3 m-3">
      <%= link_to chatroom.name, chatroom_path(chatroom), class: "btn" %>
    </div>
    <% end %>
</div>